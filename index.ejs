<%- include('layout') %>
<section>
    <% posts.forEach(post => { %>
        <div class="post">
            <h2><%= post.title %></h2>
            <p><%= post.content %></p>
        </div>
    <% }) %>
</section>
